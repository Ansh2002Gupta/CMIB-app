import colors from "../../assets/colors";

const commonStyles = {
  headerContainer: {
    position: "sticky",
    top: 0,
    zIndex: 1,
    backgroundColor: colors.white,
  },
};

export default commonStyles;