import {
  BrowserRouter as Router,
  Navigate,
  Outlet,
  useNavigate,
  useParams,
  useRoutes,
  useLocation,
  useSearchParams,
} from "react-router-dom";

export {
  Router,
  Navigate,
  Outlet,
  useLocation,
  useNavigate,
  useParams,
  useRoutes,
  useSearchParams,
};
