const styles = (theme) => {
  const { colors } = theme;

  return {
    container: {
      flex: 1,
    },
    innerContainer: {
      padding: 24,
      flex: 1,
    },
    flexGrow: {
      flexGrow: 1,
    },
  };
};

export default styles;
