const getStyles = (theme) => {
  const { colors } = theme;

  return {
    mainViewStyle: {
      left: 0,
      right: 0,
      bottom: 0,
      top: 0,
      position: "fixed",
    },
  };
};

export default getStyles;
