import React from "react";
import View, { Text } from "@unthinkable/react-core-components";

const Slider = () => {
  return (
    <View>
      <Text>Slider</Text>
    </View>
  );
};

export default Slider;
