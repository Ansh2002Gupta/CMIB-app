const getStyles = (theme) => {
  return {
    imageStyle: {
      flex: 1,
      justifyContent: "center",
      position: "absolute",
      bottom: 0,
      overflow: "hidden",
      width: "100%",
    },
  };
};

export default getStyles;
