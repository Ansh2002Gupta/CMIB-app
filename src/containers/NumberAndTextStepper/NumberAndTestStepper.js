import React from "react";

import NumberAndTextStepperTemplate from "./NumberAndTextStepperTemplate";

const NumberAndTextStepper = ({ stepperData, selectedStepper }) => {
  return <NumberAndTextStepperTemplate stepperData={stepperData} selectedStepper={selectedStepper} />;
};

export default NumberAndTextStepper;
