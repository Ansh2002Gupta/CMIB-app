import React from "react";

import CommonText from "../../../components/CommonText";

const PaymentHistory = ({ intl, isWebView }) => {
  return <CommonText>PaymentHistory</CommonText>;
};

export default PaymentHistory;
