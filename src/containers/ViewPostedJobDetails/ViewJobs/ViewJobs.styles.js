const getStyles = (theme) => {
  const { colors } = theme;

  return {
    container: {
      flex: 1,
    },
    backgroundColorWhite: {
      backgroundColor: "white",
    },
  };
};

export default getStyles;
