const getStyles = (theme) => {
  const { colors } = theme;

  return {
    customTableStyle: {
      backgroundColor: colors.white,
    },
    zIndex2: {
      zIndex: 2,
    },
    flex1: {
      flex: 1,
    },
  };
};

export default getStyles;
